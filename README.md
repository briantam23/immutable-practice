# Immutable.js Practice

## Setting up

### Dependencies

* [Immutable](https://immutable-js.github.io/immutable-js)

### Installation

* `npm install` (or `yarn install`)
* `npm start` (or `node ./index.js`)